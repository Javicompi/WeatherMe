<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        
        <variable
            name="clickListener"
            type="com.example.android.weatherme.ui.list.CurrentListener" />
        
        <variable
            name="current"
            type="com.example.android.weatherme.data.database.entities.current.CurrentEntity" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_height"
        android:layout_margin="@dimen/list_item_current_margin"
        app:cardCornerRadius="@dimen/list_item_corner_radius">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="@{() -> clickListener.onClick(current)}">

            <ImageView
                android:id="@+id/list_current_icon"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:currentIcon="@{current.icon}"
                android:elevation="2dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHeight_percent="0.5"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintWidth_percent="0.25"
                tools:src="@drawable/_01d"/>

            <TextView
                android:id="@+id/list_current_cityName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/small_margin"
                android:elevation="2dp"
                android:text="@{current.cityName}"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Headline3"
                android:textSize="@dimen/list_item_current_location_text"
                app:layout_constraintEnd_toStartOf="@id/list_current_icon"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Gran Alacant" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>

</layout>